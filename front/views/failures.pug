extends layout

block content
  .container
    .row
      .col-md-12
        .list
          ul
            each failure in failures
              li
                a(href="/readOne/" + failure.id)
                  .title= failure.title
                .symptom= failure.symptoms
                .author.text-right by
                  span &nbsp;sirodoht
